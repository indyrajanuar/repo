from sklearn.preprocessing import PolynomialFeatures
from sklearn.linear_model import LinearRegression
import pickle

# ... kode pelatihan model Anda ...

# Membuat objek PolynomialFeatures dengan derajat 2
poly = PolynomialFeatures(degree=2)

# Melakukan ekspansi fitur pada data pelatihan
X_train_expanded = poly.fit_transform(X_train)

# Melatih model menggunakan data yang telah diperluas
model = LinearRegression()
model.fit(X_train_expanded, y_train)

# Menyimpan model yang telah diperluas
model_data = {
    'model': model,
    'X_train_expanded': X_train_expanded,
    'y_train': y_train
}

with open('model.pkl', 'wb') as file:
    pickle.dump(model_data, file)